settings: false
phab_users: []
github_users: []
phabricator_config:
    url: ""
    api_token: ""
    api_token_env: PHAB_API_TOKEN
    access_token: ""
    access_token_env: PHAB_ACCESS_TOKEN
    arrc_file_path: ~/.arcrc


# TODO(moises): create a better yaml file.

apis:
    phabricator:
        base_url: "some"
        api_token_env: PHAB_API_TOKEN
        api_token: ""
        access_token_env: PHAB_ACCESS_TOKEN
        arrc_file_path : ~.arcrc

    github:
        base_url:
        api_token_env:
        api_token:

me:
    phab: user
    github: user

teams:
    a_team:
        phab_users:
            - user_one
            - user_two
        github_users:
            - user_one
            - user_two
